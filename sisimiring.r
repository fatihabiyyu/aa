A <- as.numeric(readline(prompt = "Masukkan nilai sisi alas: "));
B <- as.numeric(readline(prompt = "Masukkan nilai sisi tegak: "));
C <- sqrt(A * A + B * B);
cat("Sisi miringnya adalah:", C, "\n");

